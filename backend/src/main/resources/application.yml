spring:
  application:
    name: ssuai
  profiles:
    default: dev

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never

ssuai:
  connector:
    meal: mock # mock | real (real implemented in Task 03)
    dorm-meal: mock # mock | real (real implemented in Task 04)
