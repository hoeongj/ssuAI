# Local dev: point at the Spring Boot backend running on localhost
NEXT_PUBLIC_SSUAI_API_BASE=http://localhost:8080

# Production (Vercel): set this in the Vercel project settings → Environment Variables
# NEXT_PUBLIC_SSUAI_API_BASE=https://ssumcp.duckdns.org
